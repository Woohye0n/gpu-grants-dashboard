requests>=2.28
olefile>=0.46          # 바이너리 .hwp (HWP 5.x, OLE 컨테이너) 텍스트 추출
PyMuPDF>=1.23          # PDF 텍스트 추출 (sort=True 로 한글·숫자 뒤섞임을 막는다)
pypdf>=3.0             # PyMuPDF 를 못 쓸 때의 대비책
beautifulsoup4>=4.11   # scraper/probe.py — 게시판 목록 구조 탐지
lxml>=4.9              # 위 파서 백엔드
